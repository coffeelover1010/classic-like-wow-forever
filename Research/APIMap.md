# API map

The initial APIs are centralized in `Data/APIManifest.lua`: unit health/power, name/level, `GetBuildInfo`, `C_Texture.GetAtlasInfo`, and combat-lockdown probing. Their client availability is a diagnostic hypothesis until runtime tested on each flavour.

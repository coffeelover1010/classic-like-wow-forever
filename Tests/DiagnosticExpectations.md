# Diagnostic expectations

On Retail development builds, `/cf environment` must report Retail/Mainline and
`/cf diagnostic` must not throw. Frame and asset availability is informative:
missing candidates are a research finding, not a failed installation.
